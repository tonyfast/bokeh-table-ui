dispatch:
  init:
    table:
    footer:

  table-click:
    table:

  row-click:
    update-axis:  !!js.function
      function(node){
        //show the table pane
        d3.selectAll('tr').classed('current-axis',false)
        d3.select(node.parentNode)
            .classed('current-axis',true)
      })

  # SHow the bokeh plot
  bokeh-click:
